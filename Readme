Tic Tac Toe with Box

 Overview
This is a simple Java implementation of the classic Tic Tac Toe game. It allows two players to take turns placing their symbols ('X' and 'O') on a 3x3 grid until one of them wins or the game ends in a draw.

 Features
- Players can input their moves via the console.
- The game checks for win conditions after each move.
- It terminates when a player wins or the game ends in a draw.
- Basic ASCII representation of the game board is displayed.

 How to Run
To run the game, follow these steps:
1. Make sure you have Java installed on your system.
2. Compile the Java files using `javac Main.java`.
3. Run the compiled program using `java Main`.

 How to Play
- Each player takes turns to input their move.
- Players enter the row and column indices (0-indexed) where they want to place their symbol.
- The game continues until one player wins or the board is filled.

Contributors
- Jarrar
